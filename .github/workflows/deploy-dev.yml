name: build-deploy-dev
on:
  push:
    paths-ignore:
      - "**.md"
      - ".gitignore"
      - "LICENCE"
      - "CODEOWNERS"
    branches:
      - master
      - feature/**
      - petterkraabol-java-25-kotlin-2-4-10-spring-boot-4
jobs:
  call-workflow:
    uses: navikt/pam-deploy/.github/workflows/deploy-dev.yml@v9
    permissions:
      actions: read
      contents: write
      id-token: write
      security-events: write
    with:
      JAVA_VERSION: 25
      BUILD_CACHE: maven
      NAIS_RESOURCE: naiserator.yml
      SKIP_DRAFT_RELEASE: ${{ github.ref_name != 'master' }}
    secrets:
      NAIS_WORKLOAD_IDENTITY_PROVIDER: ${{ secrets.NAIS_WORKLOAD_IDENTITY_PROVIDER }}
